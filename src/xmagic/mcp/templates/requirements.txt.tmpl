# Read by xMagic's hosted deployment (`pip install -r requirements.txt`).
# Rendered from the same list as pyproject.toml's dependencies; edit both or
# regenerate rather than letting them drift.
{dependencies_txt}
