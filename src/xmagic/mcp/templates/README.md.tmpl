# {name}

An MCP server for xMagic custom tools, generated by `xmagic mcp init`.

## Run locally

```bash
docker compose up --build
# MCP endpoint: http://localhost:8000/mcp
```

## Expose over HTTPS for development

xMagic requires a **public HTTPS** URL. For development, use a tunnel:

```bash
cloudflared tunnel --url http://localhost:8000
# or: ngrok http 8000
```

## Register with xMagic

1. Open xMagic -> sidebar -> **Custom tools** -> **Create tool**
2. Name + description (shown in Studio)
3. Server URL: `https://<your-domain>/mcp`
4. API key: the value of `TOOL_API_KEY` (recommended; rotate regularly)
5. Attach the tool to a Job in your Agent's Studio and deploy

## Host on xMagic (upcoming)

xMagic-hosted deployment is on the platform roadmap and is not available yet.
When it lands, the hosted runtime will unpack a zip of this directory, install
`requirements.txt`, and run `mcp_server.py` from the project root. Both files
are generated here already and start the same server as `docker compose up`,
so nothing about this project needs to change. Give the deployment `/health`
as its health path — it answers without a key. Until then, deploy the
container yourself as described below.

## Production checklist

- [ ] Deploy on xMagic, or run the container (Cloud Run, Fly.io, ECS, k8s, ...) behind HTTPS
- [ ] Set `TOOL_API_KEY` and register the same value in xMagic
- [ ] Return well-structured JSON from every tool
- [ ] Keep request logging on for debugging; add timeouts to upstream calls
